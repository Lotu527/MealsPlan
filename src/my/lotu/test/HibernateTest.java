package my.lotu.test;

import org.junit.Test;

public class HibernateTest {
	@Test
	public void test(){
		/*Session session=new FoodDAO().getSession();
		Food food=new Food("梅菜扣肉","主食","菜肴",15.0,222.8,332.5,223.6,1020.0,300.0);
		try{
			session.beginTransaction();
			session.merge(food);
			session.getTransaction().commit();
		}catch(Exception e){
			e.printStackTrace();
			session.getTransaction().rollback();
		}*/
	}
}
